service: aws-serverless-node
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: eu-central-1
  environment:
    ENV1: "ENV1_VALUE"

functions:
  starter:
    handler: index.handler
    environment:
      ENV2: "ENV2_VALUE"
